# Product SDD Preset

This preset composes Agile governance checks into `speckit.specify`,
`speckit.plan`, `speckit.tasks`, `speckit.analyze`, and
`speckit.implement`.

Install the bundled `agile` extension first, initialize
`.product/`, then install this preset. Removing the preset restores the prior
command resolution and preserves all product artifacts.

